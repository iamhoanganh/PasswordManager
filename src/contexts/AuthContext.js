import { createContext } from 'react'

/**
 * Context for storing Authentication state (No actual mission critical data such as passwords are stored here)
 */
export const AuthContext = createContext();